rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Safe default: deny all access until project-specific rules are defined.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
